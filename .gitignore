# Secrets
.env
backend/.env
frontend/.env.local
frontend/.env

# Local editor/assistant tooling config, not part of the published repo
.claude/
CLAUDE.md
# The long narrative architecture write-up, kept local. docs/architecture.md is
# the version the repo publishes and the one to keep current.
/ARCHITECTURE.md

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.venv/
venv/

# Node
node_modules/
dist/
.next/
# Emitted by `tsc -b` (composite project references), not source.
*.tsbuildinfo
frontend/vite.config.js
frontend/vite.config.d.ts

# OS
.DS_Store
Thumbs.db

# Python packaging
*.egg-info/

# Generated eval reports (the *_golden_set.json fixtures are the only tracked evals JSON)
backend/report.json
backend/evals/*.json
!backend/evals/scorer_golden_set.json
!backend/evals/generator_golden_set.json
